# =============================================================================
# Sanity CMS
# =============================================================================
# Public — exposed to the browser (NEXT_PUBLIC_ prefix)
# Required. Used by: sanity/env.ts, lib/sanity.client.ts, lib/sanity.server.ts
NEXT_PUBLIC_SANITY_PROJECT_ID=yourProjectId
NEXT_PUBLIC_SANITY_DATASET=production
# Optional (defaults to 2024-01-01)
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Server-only — never exposed to the browser
# Required. Used by: lib/sanity.server.ts (contact form writes to Sanity)
SANITY_WRITE_TOKEN=yourWriteToken

# =============================================================================
# Google Analytics 4
# =============================================================================
# Recommended. Used by: lib/ga.ts, src/app/layout.tsx
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# Site URL (used for canonical links, OG images, sitemaps)
# =============================================================================
# Recommended. Used by: lib/seoConfig.ts (defaults to https://shootstudio.in)
# Set this to your Firebase Hosting URL, e.g. https://<project-id>.web.app
NEXT_PUBLIC_SITE_URL=https://your-domain.com
